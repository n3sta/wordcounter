import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
  float: right;
  display: flex;
  flex-wrap: wrap;
  align-items: center;
`;

const CounterBarClass = ({ format }) => (
  <Wrapper>
      asd
  </Wrapper>
)

export default CounterBarClass;
