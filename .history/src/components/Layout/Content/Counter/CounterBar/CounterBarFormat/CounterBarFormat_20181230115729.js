import React, { Component } from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
  display: flex;
  flex-wrap: wrap;
  align-items: center;
`;

const CounterBarFormatClass = () => (
  <Wrapper>
    <button type="button" onClick={clear}
  </Wrapper>
)

export default CounterBarFormatClass;
